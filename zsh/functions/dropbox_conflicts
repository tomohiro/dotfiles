#/usr/bin/env zsh

function dropbox_show_conflicted_files() {
    find ~/Dropbox -name "*conflicted copy*"
}

function dropbox_remove_conflicted_files() {
    find ~/Dropbox -name "*conflicted copy*" -print0 | xargs -0 rm -f
}
