#/usr/bin/env zsh

function dropbox_show_conflicted_files() {
    find ~/Dropbox -name "*問題のある*"
}

function dropbox_remove_conflicted_files() {
    find ~/Dropbox -name "*問題のある*" -print0 | xargs -0 rm -f
}
