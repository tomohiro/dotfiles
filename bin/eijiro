#!/usr/bin/env ruby

$: << File.expand_path('../lib', __FILE__)
require 'translate'

if __FILE__ == $0
  text = STDIN.isatty ? ARGV : STDIN.readlines
  abort 'Please enter the text.' if text.count == 0

  puts Translate::eijiro text.join ' '
end
