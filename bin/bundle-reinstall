#!/bin/sh

if [ -d .bundle ]; then
  echo 'Remove .bundle...'
  rm -rf .bundle
fi

if [ -d vendor/bundle ]; then
  echo 'Remove vendor/bundle'
  rm -rf vendor/bundle
fi

if [ -f Gemfile ]; then
  echo 'Start bundler re:Install'
  bundle install --path vendor/bundle
fi
