#!/bin/sh

set -e

echo "===> Clenaup Homebrew"
brew cleanup

echo "===> Cleanup Vagrant Boxes"
vagrant global-status --prune

echo "===> Clenaup Docker Images"
docker-clean

echo "===> Cleanup RubyGems"
gem clean
