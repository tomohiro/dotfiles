#!/bin/sh

repo=$(git config remote.origin.url | cut -d ':' -f 2)
branch=$(git symbolic-ref --short HEAD)

if [ $branch == 'master' ]; then
  open "https://bitbucket.org/${repo}"
else
  open "https://bitbucket.org/${repo}/branch/${branch}"
fi
