# Install vmware-tools on RHEL
mount /media/cdrom
cd /media/cdrom
rpm -ivh VMwareTools-4.0.0-208167.i386.rpm
/usr/bin/vmware-config-tools.pl -d
/etc/init.d/network stop
rmmod pcnet32
rmmod vmxnet
modprobe vmxnet
/etc/init.d/network start


# Uninstall vmware-tools on RHEL
rpm -e VMwareTools
