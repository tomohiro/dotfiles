# 英語でセットアップして ssh した時に warning: setlocale: LC_ALL: cannot change locale (ja_JP.UTF-8) とか出る場合
$ sudo apt-get install language-pack-ja -y

# getty の tty を停止させる
$ sudo initctl stop tty3 # tty3 を停止, initctl list でジョブ一覧を確認

# Ubuntu の環境変数を編集する(http_proxy など)
$ sudo vim /etc/environment
