# commit 済み,または add してしまったファイルを管理対象から外す
git rm --cached [file-name] # rm だがファイルは削除されない

# rebase に失敗して `Interactive rebase already started` と言われる場合
git rebase --abort  # 再度 git rebase する

# Delete a tag on the GitHub
git push --delete origin branch-name   # Git 1.7
git push origin :branch-name           # Git 1.6

# Delete a tag on the GitHub
git push --delete origin tag-name   # Git 1.7
git push origin :tag-name           # Git 1.6


# Remote URL の変更 origin の url を変更する場合
git config remote.origin.url git@github.com:Tomohiro/hoge.git

# 特定のファイルが未コミットで編集されていた場合，最後のコミットまで戻す
git checkout -- path/to/file

# hub コマンドで github から fork して pull request をさくっと - #生存戦略 、それは - subtech
http://subtech.g.hatena.ne.jp/secondlife/20120611/1339411825

# Remote の最新まで戻す
git reset --hard ORIG_HEAD
