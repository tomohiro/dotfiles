# 複数ファイルのメソッド名を置換する
git grep -n 'foo_method' app/models/*.rb > target-methods # 対象メソッドの一覧を得る
vi target-methods                                         # foo_method を bar_method に置換
geed-repl < cat target-methods | ed                       # ed を実行してメソッド名を置換
git diff                                                  # diff で実行結果を確認する
